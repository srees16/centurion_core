# The pinned core: everything the NSE engine, the daily paper session, the
# tests, the heartbeat and the maintenance jobs need.
#
# requirements.txt includes this file and adds only what the web application
# needs on top, so a package is pinned in exactly one place.
#
# Why pinned: nothing was, and on 25 Sep 2026 a new SQLAlchemy (2.1) changed
# which driver a bare postgresql:// URL resolves to. Every paper run died
# before reaching the book, and that day's queued orders were lost. The
# versions below are the ones the book has been trading on, taken from the
# Actions install logs, so pinning changed no behaviour.
#
# To move a pin: change it, run the paper workflow by hand, and check the
# session completes and the daily email arrives.
beautifulsoup4==4.15.0
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
curl_cffi==0.16.3
idna==3.20
iniconfig==2.3.0
lxml==6.1.3
multitasking==0.0.13
numpy==2.4.6
packaging==26.3
pandas==3.0.6
peewee==4.5.1
platformdirs==4.11.13
pluggy==1.6.0
protobuf==7.36.2
psycopg2-binary==2.9.13
pyarrow==25.0.1
pycparser==3.0
Pygments==2.21.0
pytest==9.1.1
python-dateutil==2.9.0.post0
python-dotenv==1.2.3
pytz==2026.4
requests==2.34.2
scipy==1.17.1
six==1.17.0
soupsieve==2.10
SQLAlchemy==2.0.54
typing_extensions==4.16.0
urllib3==2.8.0
websockets==17.1
yfinance==1.7.0
