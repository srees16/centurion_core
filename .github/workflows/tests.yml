name: Tests

# Fast tests only: anything marked `slow` needs the parquet store, which is
# gitignored and lives in the paper-trading job's cache.
on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pytest:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install the engine's dependencies
        run: |
          python -m pip install --upgrade pip
          pip install numpy pandas pyarrow scipy sqlalchemy pytest
      - name: Run the fast suite
        run: python -m pytest tests -q -m "not slow"
